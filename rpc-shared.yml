version: '3.4'

# Include this file in COMPOSE_FILE and set EXPOSE_RPC=true in .env to expose the ETH RPC port
services:
  node:
    ports:
      - "${ETH_RPC_PORT:-3001}:${ETH_RPC_PORT:-3001}"   # ETH RPC Port
    # Gossip ports are now always exposed in hyperliquid.yml
    # - "${GOSSIP_PORT_1:-4001}:${GOSSIP_PORT_1:-4001}" # Gossip Port 1
    # - "${GOSSIP_PORT_2:-4002}:${GOSSIP_PORT_2:-4002}" # Gossip Port 2